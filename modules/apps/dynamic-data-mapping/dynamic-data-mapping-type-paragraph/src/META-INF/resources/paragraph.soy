{namespace ddm}

/**
 * Prints the DDM form paragraph field.
 *
 * @param dir
 * @param fieldQualifiedName
 * @param fieldValue
 */
{template .paragraph}
	<p dir="{$dir}" id="{$fieldQualifiedName}">
		{$fieldValue}
	</p>
{/template}