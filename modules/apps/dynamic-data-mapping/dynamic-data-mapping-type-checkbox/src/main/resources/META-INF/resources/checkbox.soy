{namespace ddm}

/**
 * Prints the DDM form checkbox field.
 *
 * @param childElementsHTML
 * @param label
 * @param name
 * @param options
 * @param readOnly
 * @param required
 * @param showAsSwitcher
 * @param showLabel
 * @param status
 * @param visible
 */
{template .checkbox}
	<div class="form-group form-inline{$visible ? '' : ' hide'} liferay-ddm-form-field-checkbox" data-fieldname="{$name}">
		{if $showAsSwitcher}
			<div class="checkbox">
				<label class="control-label" for="{$name}">
					<input class="hide toggle-switch" {if $readOnly}disabled{/if} id="{$name}" name="{$name}" type="checkbox" {$status} value="true" />

					<span aria-hidden="true" class="toggle-switch-bar">
						<span class="toggle-switch-handle">
							<span class="toggle-switch-text">
								{$label}

								{if $required}
									<b>*</b>
								{/if}
							</span>
						</span>
					</span>
				</label>
			</div>
		{else}
			<div class="checkbox checkbox-default">
				{if $showLabel}
					<div>{$label} {if $required}<b>*</b>{/if}</div>
				{/if}

				{foreach $option in $options}
					<div>
						<label class="checkbox-inline" for="{$option.value}">
							<input {if $readOnly}disabled{/if} id="{$option.name}" name="{$option.name}" type="checkbox" {$option.status} value="{$option.value}" />

							{$option.label}
						</label>
					</div>
				{/foreach}
			</div>
		{/if}

		{$childElementsHTML|noAutoescape}
	</div>
{/template}